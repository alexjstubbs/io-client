let navigationBindings = require("./io-keyboard-events");

    /* Bind local Navigation
    -------------------------------------------------- */
    navigationBindings
        .navigationEventListeners
        .bindEventNavigation();
